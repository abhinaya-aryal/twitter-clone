// ! This file is used as the alternatives to head component of next js. We can define title and other meta tag too.

const RootHead = () => {
  return <title>Home / Twitter</title>;
};

export default RootHead;
